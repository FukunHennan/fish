# 机器鱼项目统一手册

更新日期：2026-09-16

本手册是 Fish Control 项目的唯一主文档，统一收录项目架构、账号、部署、设备控制、视觉、赛事、通信协议、OTA、测试、诊断和维护规范。以前的分散说明已合并至本文档；发生冲突时，以当前代码、自动化测试和实机验收结果为准。

## 1. 项目概览

Fish Control 是一套由现场电脑统一管理多台 ESP32 机器鱼、视觉识别和学生赛事的系统。

```text
浏览器 GUI
   ↓ HTTP / SSE / WebRTC
Go Controller（0.0.0.0:8081）
   ├─ ESP32 机器鱼（WebSocket /ws/device）
   └─ Python Vision（127.0.0.1:8091）
          └─ 摄像头、YOLO、跟踪、循迹和视频
```

核心边界：

- 浏览器不直接连接 ESP32，也不直接访问 Python。
- Go 是登录、权限、设备状态、控制权、命令排队和视觉代理的唯一入口。
- Python 负责摄像头采集、识别、跟踪和视觉控制计算，不直接访问机器鱼 IP。
- 设备在线、手动控制和视觉状态是三个独立概念。
- 设备真实状态以 ESP32 的 `heartbeat`、`state` 和 `command.result` 为准。

## 2. 目录和入口

| 目录 | 用途 |
| --- | --- |
| `controller/` | Go 控制器、Web API、设备 Hub 和 React GUI |
| `controller/frontend/src/` | 正式前端源码 |
| `controller/frontend/public/competition/` | 赛事端静态兼容界面 |
| `vision/` | Python 视觉、YOLO、WebRTC 和循迹 |
| `firmware/` | ESP32-C3 固件、配网、运动、RGB 和 OTA |
| `protocol/` | 协议目录；当前协议说明已合并到本手册 |
| `config/` | 本机部署配置与模板 |
| `scripts/` | 启停、体检、测试和固件辅助脚本 |
| `design/gui/` | 设计稿、原型和历史视觉材料 |
| `docs/` | 本统一手册及必要的对外成品 |

正式界面：`http://127.0.0.1:8081/`

前端开发界面：`http://127.0.0.1:8098/`

赛事界面：`http://127.0.0.1:8081/competition.html`

Cloudflare 公网入口：`https://fish.chenfukun.space/`

Cloudflare 公网赛事入口：`https://fish.chenfukun.space/competition.html`

## 3. 当前赛事账号

账号库：`/home/chenfukun/.config/fish-controller/users.json`

| 用途 | 短账号 | 完整账号 | 显示名称 | 密码 | 权限 |
| --- | --- | --- | --- | --- | --- |
| 裁判 | `referee` | `referee@fish.local` | `赛事裁判` | `123456789` | 管理员 |
| 蓝队 | `team-blue` | `team-blue@fish.local` | `蓝队` | `123456789` | 普通用户 |
| 红队 | `team-red` | `team-red@fish.local` | `红队` | `123456789` | 普通用户 |


登录时默认使用短账号。前端和后端都会将不含 `@` 的账号补全为 `@fish.local`，完整账号仍然兼容。当前席位分配：B1/B2 使用蓝队账号，R1/R2 使用红队账号。

## 4. 启动、停止与桌面入口

推荐使用统一管理脚本：

```bash
python3 scripts/fishctl status
python3 scripts/fishctl start
python3 scripts/fishctl stop
python3 scripts/fishctl restart
python3 scripts/fishctl open
python3 scripts/fishctl logs
python3 scripts/fishctl doctor
```

安装桌面菜单入口：

```bash
python3 scripts/fishctl install-desktop
```

安装后搜索 `Fish Controller`。桌面入口会启动服务、等待健康检查，然后打开 `8081` 正式控制台。

如果本机已安装 `fish-controller.service`，`fishctl` 会自动使用 `systemctl --user`。不要同时再运行 `scripts/start.sh`，否则会启动第二份进程。

```bash
systemctl --user status fish-controller.service
systemctl --user status fish-gui.service
systemctl --user status fish-cloudflared.service
```

当前三个服务都注册为开机自启：

```bash
systemctl --user enable --now fish-controller.service fish-gui.service fish-cloudflared.service
loginctl enable-linger "$USER"
```

`fish-controller.service` 提供 `8081` 正式服务，`fish-gui.service` 在正式前端目录提供 `8098` 开发热更新，`fish-cloudflared.service` 提供 Cloudflare 公网入口。启用 lingering 后，用户未登录桌面时这些用户服务也可随系统启动。

前端开发：

```bash
cd controller/frontend
npm run dev -- --host 127.0.0.1
```

生产构建：

```bash
npm --prefix controller/frontend run build
```

构建产物位于 `controller/internal/web/dist` 并由 Go 嵌入。只修改源码或重新构建前端，不会自动替换 systemd 正在运行的 Go 二进制文件。

## 5. 端口与统一参数

| 项目 | 当前值 |
| --- | --- |
| Go Controller | `8081/TCP` |
| Vite 开发 GUI | `8098/TCP` |
| Python Vision | `127.0.0.1:8091/TCP` |
| MJPEG 内部回退 | `8090/TCP` |
| 局域网发现 | `30303/UDP` |
| 平板遥测 | `9998/TCP` |
| 控制权运动活动期 | `60 s` |
| 视觉运动命令有效期 | `3 s` |
| Go 设备离线判断 | 约 `3 s` |
| 设备/控制器心跳 | 约 `1 s` |
| 默认摄像头采集 | `1920×1080`，目标 `30 FPS` |
| YOLO 默认输入 | `1920` |
| 视频优先传输 | WebRTC |

视觉采集可用 `FISH_CAPTURE_WIDTH`、`FISH_CAPTURE_HEIGHT` 和 `FISH_YOLO_IMGSZ` 覆盖，需重启视觉服务生效。

## 6. 账号、归属与控制会话

系统分为三层身份：

1. 网页账号：用户、角色和状态。
2. 登录会话：使用 Cookie，在有效期内可跨服务重启。
3. 设备控制会话：由用户 ID、浏览器 `clientId` 和 `deviceId` 共同确定。

归属规则：

- 一名用户可以持有多条鱼。
- 同一条鱼同一时间只能归属一名用户。
- 设备离线、用户退出、运动超时和服务重启会停止运动，但不自动释放归属。
- 服务重启后保留账号归属，不恢复运动；当前浏览器需点击“在本窗口恢复控制”。
- 只有持有者主动释放或管理员强制释放才会解除归属。
- 管理员全局急停只停止运动，不释放归属。

按键持续按住时，页面约每 15 秒续租。松键、切页或窗口隐藏后停止续租并停止本窗口键盘运动。

## 7. 设备在线与手动控制

状态语义必须严格分离：

- **在线/离线**：只由设备心跳和 Go 快照决定。
- **手动控制可用**：依赖登录、归属、当前浏览器控制会话和设备在线。
- **视觉可用**：只表示摄像头、WebRTC、识别、跟踪是否正常。

没有视觉或视觉连接失败时，不能把在线机器鱼显示为离线，也不能阻断已分配在线机器鱼的手动控制。

手动动作：`forward`、`left`、`right`、`stop`、`idle`。默认频率约 `2.5 Hz`，协议频率范围 `0.3–5.0 Hz`，振幅范围 `0–90°`，偏置范围 `-90°–+90°`。Go 会按每台设备的标定范围限幅，保证完整摆动周期不超过舍机上下限。

多鱼键盘控制可为每条鱼设置独立键位，如 WASD 和 IJKL。相同键位会联动，不同键位可独立操作。

## 8. 视觉、摄像头与视频

视觉流程：

```text
选择服务器摄像头
  → 启动预览
  → 选择 YOLO 模型
  → 识别机器鱼
  → 选择识别目标
  → 绑定物理 deviceId
  → 绘制路径
  → 启动循迹
```

摄像头分为两类：

- **服务器摄像头**：连接在运行 Python Vision 的现场电脑上，用于共享视频、YOLO、跟踪和循迹。电脑自带摄像头如果能被后端打开，也会出现在该列表中。
- **电脑摄像头**：使用当前浏览器的 `getUserMedia`，可选内置或 USB 摄像头，仅供当前页面预览，不代替服务器识别和循迹。

通过 Cloudflare Tunnel 远程访问时，“电脑摄像头”属于访问者所在的电脑，不会自动传回现场 Python。Cloudflare 域名使用 HTTPS，可满足浏览器的摄像头安全环境要求；`localhost` 和 `127.0.0.1` 也属于浏览器允许的本地安全环境。

视频优先使用 WebRTC。`smooth`、`hd`、`full` 只改变当前观看连接的分辨率，不改变共享识别原图。公网优先使用 `smooth`。

管理员可在停止识别和循迹后调整识别区域裁剪。裁剪同时作用于显示和识别，会重建视觉会话，应重新确认目标和标定。

视觉运动有独立的 3 秒命令有效期。新视觉运动命令续期，设备心跳不续期。超时、手动接管、停止或 OTA 会废止旧视觉会话。

## 9. 赛事系统

比赛形式为蓝队 B1/B2 对红队 R1/R2。裁判控制比赛流程，选手只能控制裁判分配的机器鱼，服务端是比赛状态的唯一权威源。

### 9.1 标准流程

1. 裁判签到并绑定 B1/B2/R1/R2 与机器鱼。
2. 完成摄像头、裁剪、曝光、场地坐标和目标识别检查。
3. 裁判开放受限设备试动。
4. 选手分别确认自己的设备正常或异常。
5. 设备确认完成后进入等待开始。
6. 裁判开始比赛，服务端进入 `RUNNING`，开始计时并开放控制。
7. 暂停时服务端立即禁止控制并向已分配鱼发送停止。
8. 结束时固化比分、事件、视频和 JSON 记录。

### 9.2 状态机

| 状态 | 含义 | 允许选手控制 |
| --- | --- | --- |
| `CHECKIN` | 签到和绑定 | 否 |
| `FIELD_SETUP` | 场地准备 | 否 |
| `TEST` | 设备试动 | 是，受限 |
| `READY` | 等待裁判开始 | 否 |
| `RUNNING` | 比赛进行 | 是 |
| `PAUSED` | 比赛暂停 | 否 |
| `ENDED` | 比赛结束 | 否 |
| `ARCHIVED` | 记录已归档 | 否 |

当前原型计分：进球 `+10`、配合 `+2`、越界/违规 `-2`。这些是当前原型值，正式赛事应以最终竞赛规程为准。

每次计分至少记录：剩余时间、系统时间、队伍、事件名称、分值变化和 `videoTimestampSeconds`。视频与 JSON 使用同一 basename 配对存储。

断线重连后，前端必须重新获取服务端比赛快照，不能依赖浏览器本地状态恢复比赛。

## 10. 赛事前端方案

当前界面以用户提供的图片为设计文件。验收看相对比例，不锁定绝对像素。顶部导航、左右栏、中央画面、HUD、间距和底部状态条应保持设计稿比例。

限定技术路线：React、TypeScript、Vite、Motion for React、Zustand、TanStack Query、SSE/EventSource、WebRTC、CSS Modules 与全局主题 CSS。当前仓库已在 React/Vite 上运行，其余依赖在正式重构引入时再按需添加。

允许动效：Dock 磁吸放大、卡片悬停轻微放大和浮起、按钮压感、页面淡入/滑入、资源点脉冲、机器鱼悬停高亮和轨迹线轻微流光。动效只用 `transform`、`opacity`、`filter` 和 `box-shadow`，不得改变布局尺寸，并支持 `prefers-reduced-motion`。

正式前端只使用同源相对路径，不在代码中写死本机或局域网 IP。

## 11. 内网穿透与公网访问

正式链路：

```text
远程浏览器
  → Cloudflare Tunnel
  → 现场电脑 Go Controller:8081
  → Python Vision / ESP32 保持在现场
```

当前穿透服务由 systemd 用户单元 `fish-cloudflared.service` 管理，使用 token 运行 Cloudflare Tunnel。隧道只将公网流量转发到 Go `8081`，不直接暴露 Python `8091`、MJPEG `8090` 或 ESP32。

当前公网域名为 `https://fish.chenfukun.space`，Cloudflare 入口规则将该域名转发到 `http://127.0.0.1:8081`。

Cloudflare Tunnel 可转发网页、API 和 WebRTC 信令，但不代替 WebRTC 的 ICE/STUN/TURN。跨公网视频不稳定时可配置：

```text
FISH_WEBRTC_STUN_URL=stun:...
FISH_WEBRTC_TURN_URL=turn:...
FISH_WEBRTC_TURN_USERNAME=...
FISH_WEBRTC_TURN_CREDENTIAL=...
```

前端 Dock、悬停和等比布局均在浏览器本地执行，内网穿透不会改变动效本身。公网主要影响视频延迟、接口响应、命令确认和首次资源加载。

## 12. ESP32 通信协议

设备连接 Go 的 `/ws/device`，所有消息是 UTF-8 JSON，协议版本为 `1`。

### 12.1 注册

Go 先发送：

```json
{"type":"auth.challenge","protocolVersion":1,"nonce":"32位十六进制随机数"}
```

设备使用部署密钥计算 HMAC-SHA256 证明，再发送：

```json
{"type":"register","protocolVersion":1,"deviceId":"A4:CF:12:34:56:78","proof":"64位HMAC","name":"机器鱼1号","firmwareVersion":"1.3.2","ip":"192.168.1.31","capabilities":["motion","ota"]}
```

注册成功前不得执行命令。`nonce` 只用于当前连接，部署密钥不得出现在浏览器、日志或 WebSocket 消息中。

### 12.2 心跳和运动

```json
{"type":"heartbeat","mode":2,"frequency":2.5,"amplitude":28.0,"bias":0.0,"rssi":-52}
```

模式：`0=停止`、`1=待机`、`2=前进`、`3=左转`、`4=右转`。

```json
{"type":"command","requestId":"唯一编号","deviceId":"A4:CF:12:34:56:78","command":"motion.set","payload":{"controlSource":"user-or-vision-bot","mode":"forward","frequency":2.5,"amplitude":28.0,"bias":0.0}}
```

设备必须原样返回 `requestId`：

```json
{"type":"command.result","requestId":"唯一编号","success":true,"message":"OK"}
```

同一设备的手动、键盘、视觉、释放停止和急停必须经过同一发送队列。尚未写入的实时运动可合并，停止必须排在先前已接受的动作之后，且旧命令不能在停止之后再发出。

ACK 语义：`sent` 只表示已写入 WebSocket；`acknowledged` 表示收到对应的 `command.result`；`success` 表示设备报告执行成功；`queued` 只表示实时意图入队，不代表设备已执行。

## 13. 固件、配网与 OTA

默认目标为 Seeed XIAO ESP32-C3，串口波特率 `115200`。设备优先连接上次认证成功的服务器，注册失败后回到发现流程。

设备长时间无法注册时会进入 `Fish-Setup-XXXXXX` 配网热点。连接后打开 `http://192.168.4.1/`，可重新设置 Wi-Fi 或上传 XIAO ESP32-C3 的应用固件 `firmware.bin`。不要上传 `bootloader.bin`、`partitions.bin` 或合并镜像。

OTA 只能由管理员发起。设备在下载和写入期间必须停止运动。服务器请求超时不等于 OTA 一定失败，最终结果应以设备重新上线后报告的固件版本为准。

固件编译：

```bash
pio run -d firmware -e seeed_xiao_esp32c3
```

## 14. 诊断与故障排查

每次 Go 启动会在 `controller/diagnostics/runs/` 创建诊断会话：

```text
LATEST.txt
<session-id>/runtime.json
<session-id>/controller.jsonl
<session-id>/controller.txt
<session-id>/python-vision.txt
```

- `runtime.json`：启动环境和进程信息。
- `controller.jsonl`：结构化生命周期和 HTTP 事件，最适合时间线分析。
- `controller.txt`：Go 人类可读日志。
- `python-vision.txt`：摄像头、YOLO、WebRTC 和 Python 运行日志。

复现问题时，一次只复现一类问题，记录操作和大致时间，再取 `LATEST.txt` 指向的完整目录。摄像头重启问题可在同一次运行中重复“打开 → 预览 → 关闭”数次以便对齐时间戳。

常见检查：

```bash
curl http://127.0.0.1:8081/healthz
curl http://127.0.0.1:8091/health
ss -ltnp | rg ':(8081|8091|8098)\b'
python3 scripts/fishctl logs
```

健康接口正常不代表机器鱼在线；设备在线仍以注册和心跳为准。

## 15. 测试与验证

前端：

```bash
npm --prefix controller/frontend test -- --run
npm --prefix controller/frontend run build
```

Go：

```bash
cd controller
go test ./...
```

Python Vision：

```bash
python3 -m unittest discover -s vision/tests -t . -p 'test_*.py'
```

固件主机测试、嵌入式编译检查和正式编译：

```bash
pio test -d firmware -e native
pio test -d firmware -e embedded_test --without-uploading --without-testing
pio run -d firmware -e seeed_xiao_esp32c3
```

`embedded_test --without-testing` 只表示编译成功，不代表测试已在开发板上执行。涉及真实鱼运动、断网停机、摄像头、OTA 和 WebRTC 的改动，必须区分“自动化测试通过”与“实机验收通过”。

## 16. 维护规范

- 每次改动尽量只解决一个主题。
- 优先沿用现有模块边界，不在未验证时重写运动和视觉核心链路。
- 不伪造设备状态、传感器数据、OTA 进度、赛事数据或视觉数据。
- 前端的可见状态必须可追溯到后端快照或设备回报。
- 生成输出、诊断记录、模型、校准数据和运行配置不按“无引用文件”随意删除。
- 前端构建会更新 Go 嵌入目录；不要在 Vite 清空输出目录的瞬间并发执行 Go 编译。
- 修改协议、相机、OTA、WebSocket 或硬件控制时，在提交说明中写明自动化测试和实机验收的边界。

## 17. 历史结论与待验证项

已实现并有自动化覆盖的重要改进：

- Go 统一运动限幅、租约校验和单设备出站队列。
- 视觉运动 3 秒有效期与旧视觉会话废止。
- 同一账号不同浏览器不能共用当前控制会话。
- 服务重启保留设备归属，但需当前窗口明确恢复控制。
- 视频预览、服务器识别和每设备视觉工作区的生命周期分离。
- 赛事账号、席位分配和机器鱼在线状态已接入真实后端。

仍需持续实机验收：

- 舍机中位、振幅边界和不同鱼的标定一致性。
- 网络突然中断时的实际物理停机时间。
- Python 崩溃、摄像头失帧、视觉目标丢失和手动接管。
- 多设备并行、跨电脑接管和多账号视觉工作区隔离。
- OTA 失败恢复、离线固件上传和升级后版本确认。
- 公网 WebRTC 的 TURN 配置、延迟和长时间稳定性。

## 18. 扩展方向

后续能力按优先级演进：

1. 完成手动控制、视觉循迹和赛事流程的实机验收。
2. 将运动标定统一为按 `deviceId/MAC` 持久化的数据模型。
3. 改进视觉目标丢失后的稳定身份恢复。
4. 完成真实任务、积分、回放和策略部署后端。
5. 在稳定 OTA 基础上建立设备能力描述、扩展包和图形化编程层。

本项目当前面向内部研发和受控环境。工程优先级是设备功能、控制可靠性、视觉效果、异常恢复、赛事体验和文档一致性。
